{
  "openapi": "3.0.0",
  "paths": {
    "/health": {
      "get": {
        "description": "Answers as soon as the process is up. Touches no database.",
        "operationId": "checkHealth",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The service is running.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            }
          }
        },
        "summary": "Liveness probe",
        "tags": ["health"]
      }
    },
    "/users": {
      "get": {
        "description": "One page of users, newest first unless asked otherwise. For editors and above.\n\n`search` matches name, surname and email, case-insensitively, and every whitespace-separated term has to match one of them — so `леся укра` finds a Леся Українка. `role` may be repeated to accept several (`?role=ADMIN&role=EDITOR`). `sort` accepts only the properties listed on it, and `?sort=role` orders by the privilege ladder rather than alphabetically, because that is the order the Postgres enum declares.\n\nEvery parameter is optional and an empty one is read as absent, so a table can keep its controls in the URL and clear them without pruning the query string. An unrecognised parameter is a 400 rather than something ignored.",
        "operationId": "listUsers",
        "parameters": [
          {
            "name": "page",
            "required": false,
            "in": "query",
            "description": "Which page to return, counting from 1.",
            "schema": {
              "default": 1,
              "type": "integer",
              "minimum": 1,
              "maximum": 9007199254740991
            }
          },
          {
            "name": "perPage",
            "required": false,
            "in": "query",
            "description": "How many rows to return. At most 100.",
            "schema": {
              "default": 20,
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            }
          },
          {
            "name": "search",
            "required": false,
            "in": "query",
            "description": "Matched against name, surname, email, case-insensitively. Whitespace separates terms and every term has to match one of them.",
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            }
          },
          {
            "name": "sort",
            "required": false,
            "in": "query",
            "description": "Which property to order by.",
            "schema": {
              "default": "createdAt",
              "type": "string",
              "enum": ["name", "surname", "email", "role", "createdAt"]
            }
          },
          {
            "name": "order",
            "required": false,
            "in": "query",
            "description": "Which direction to sort in.",
            "schema": {
              "default": "desc",
              "type": "string",
              "enum": ["asc", "desc"],
              "example": "asc"
            }
          },
          {
            "name": "role",
            "required": false,
            "in": "query",
            "schema": {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "string",
                "enum": ["ROOT", "ADMIN", "EDITOR", "AUTHOR"],
                "description": "What the account is allowed to do. ROOT is the site's owner and there is at most one of them, ADMIN manages users, EDITOR moderates the queue, AUTHOR writes their own poems.",
                "example": "AUTHOR"
              }
            }
          },
          {
            "name": "active",
            "required": false,
            "in": "query",
            "schema": {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "boolean",
                "description": "Whether the account may sign in. A deactivated account keeps everything it has written — poems already published stay published.",
                "example": true
              }
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of users.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserPage"
                }
              }
            }
          },
          "400": {
            "description": "The query does not match the schema."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The account's role is not high enough."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "List users",
        "tags": ["users"]
      },
      "post": {
        "description": "Emails are stored lowercased and must be unique. `role` may be omitted, in which case the account is an AUTHOR; only a root account may ask for ROOT, and ROOT is a singleton, so a second one is a 409. `password` may be omitted too — the account then exists but cannot sign in until one is set.",
        "operationId": "createUser",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateUser"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The created user.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "The body does not match the schema."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The account's role is not high enough."
          },
          "409": {
            "description": "That email is taken, or a root account already exists."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Create a user",
        "tags": ["users"]
      }
    },
    "/users/{id}": {
      "get": {
        "description": "One user by id. For editors and above.",
        "operationId": "getUser",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The user's id.",
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The user.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "The id is not a UUID."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The account's role is not high enough."
          },
          "404": {
            "description": "No user has that id."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Get a user",
        "tags": ["users"]
      },
      "patch": {
        "description": "Changes only the fields present in the body; at least one is required. Ids, timestamps and passwords are not writable here. Promoting an account to ROOT requires both that no other account holds it and that the caller is the root; so does editing the root account itself. Sending `active: false` retires an account: it keeps everything it has written, every session for it ends immediately, and it can no longer sign in. Nobody may do that to their own account.",
        "operationId": "updateUser",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The user's id.",
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateUser"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated user.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "The id or the body does not match the schema."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The account's role is not high enough."
          },
          "404": {
            "description": "No user has that id."
          },
          "409": {
            "description": "That email is taken, or a root account already exists."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Update a user",
        "tags": ["users"]
      },
      "delete": {
        "description": "Permanent, and only available for an account that has left nothing behind: an account with poems or moderation history is refused with a 409, and retiring it with `active: false` is what applies to it instead. Deleting a user that is already gone is a 404, not a no-op. The root account can only be deleted by itself.",
        "operationId": "deleteUser",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The user's id.",
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The user is gone."
          },
          "400": {
            "description": "The id is not a UUID."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The account's role is not high enough."
          },
          "404": {
            "description": "No user has that id."
          },
          "409": {
            "description": "The account has poems or moderation history. Deactivate it instead."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Delete a user",
        "tags": ["users"]
      }
    },
    "/auth/register": {
      "post": {
        "description": "Creates an account and signs it in. New accounts are always AUTHOR — this endpoint has no way to ask for anything else. The response carries a short-lived access token; the refresh token is set as an httpOnly cookie.",
        "operationId": "register",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Register"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The new session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "400": {
            "description": "The body does not match the schema."
          },
          "409": {
            "description": "That email is already registered."
          }
        },
        "summary": "Register an account",
        "tags": ["auth"]
      }
    },
    "/auth/login": {
      "post": {
        "description": "Exchanges an email and password for a session. Every failure — unknown address, no password set, wrong password — is the same 401 taking the same time, so this endpoint cannot be used to discover who has an account.",
        "operationId": "login",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Login"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The new session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "400": {
            "description": "The body does not match the schema."
          },
          "401": {
            "description": "The credentials are not valid."
          }
        },
        "summary": "Sign in",
        "tags": ["auth"]
      }
    },
    "/auth/refresh": {
      "post": {
        "description": "Reads the refresh cookie and issues a new access token, re-setting the cookie with a fresh lifetime. The role comes from the account as it is now, so a change of role takes effect on the next renewal rather than when the cookie eventually expires.",
        "operationId": "refresh",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The renewed session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "401": {
            "description": "The cookie is missing, expired, or revoked."
          }
        },
        "security": [
          {
            "moodnight_refresh": []
          }
        ],
        "summary": "Renew the access token",
        "tags": ["auth"]
      }
    },
    "/auth/logout": {
      "post": {
        "description": "Revokes every refresh token for the account — on every device, which is the deliberate trade for storing no sessions — and clears the cookie. Access tokens already issued keep working until they expire, which is minutes.",
        "operationId": "logout",
        "parameters": [],
        "responses": {
          "204": {
            "description": "The session is over."
          },
          "401": {
            "description": "The cookie is missing, expired, or already revoked."
          }
        },
        "security": [
          {
            "moodnight_refresh": []
          }
        ],
        "summary": "Sign out",
        "tags": ["auth"]
      }
    },
    "/auth/me": {
      "get": {
        "description": "Read from the database on every call, so it is never a stale copy of the token.",
        "operationId": "getCurrentUser",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The current user.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "401": {
            "description": "No valid access token."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "The signed-in account",
        "tags": ["auth"]
      }
    },
    "/poems": {
      "get": {
        "description": "One page of published poems, newest first unless asked otherwise. Public — no token required, and drafts are unreachable through it.\n\nEach poem arrives with a teaser rather than its body: the first few lines, plus `truncated` saying whether there is more. The whole text is `GET /poems/{slug}`.\n\n`search` matches title and subtitle, case-insensitively, and every whitespace-separated term has to match one of them. Searching *inside* poems is deliberately not offered here — that is Postgres full-text, and it arrives beside this rather than inside it.\n\n`tag` and `author` take slugs and may each be repeated, which reads as 'any of these' — `?tag=sakralne&tag=balady` is both themes, not their intersection. `featured=true` narrows to what an editor has put on the front page.\n\nEvery parameter is optional and an empty one is read as absent. An unrecognised parameter is a 400 rather than something ignored.",
        "operationId": "listPoems",
        "parameters": [
          {
            "name": "page",
            "required": false,
            "in": "query",
            "description": "Which page to return, counting from 1.",
            "schema": {
              "default": 1,
              "type": "integer",
              "minimum": 1,
              "maximum": 9007199254740991
            }
          },
          {
            "name": "perPage",
            "required": false,
            "in": "query",
            "description": "How many rows to return. At most 100.",
            "schema": {
              "default": 20,
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            }
          },
          {
            "name": "search",
            "required": false,
            "in": "query",
            "description": "Matched against title, subtitle, case-insensitively. Whitespace separates terms and every term has to match one of them.",
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            }
          },
          {
            "name": "sort",
            "required": false,
            "in": "query",
            "description": "Which property to order by.",
            "schema": {
              "default": "publishedAt",
              "type": "string",
              "enum": ["publishedAt", "readCount", "title"]
            }
          },
          {
            "name": "order",
            "required": false,
            "in": "query",
            "description": "Which direction to sort in.",
            "schema": {
              "default": "desc",
              "type": "string",
              "enum": ["asc", "desc"],
              "example": "asc"
            }
          },
          {
            "name": "featured",
            "required": false,
            "in": "query",
            "schema": {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "boolean",
                "description": "Whether an editor has put it on the front page.",
                "example": false
              }
            }
          },
          {
            "name": "tag",
            "required": false,
            "in": "query",
            "schema": {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "string",
                "maxLength": 80,
                "description": "A tag's slug. Repeatable.",
                "example": "sakralne"
              }
            }
          },
          {
            "name": "author",
            "required": false,
            "in": "query",
            "schema": {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "string",
                "maxLength": 120,
                "description": "An author's slug. Repeatable.",
                "example": "orysia-vechirnia"
              }
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of poems.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PoemPage"
                }
              }
            }
          },
          "400": {
            "description": "The query does not match the schema."
          }
        },
        "summary": "List published poems",
        "tags": ["poems"]
      },
      "post": {
        "description": "Creates a poem owned by the account making the request. There is no field for an author — a poem belongs to whoever is holding the token.\n\n`status` may be omitted, in which case the poem is a DRAFT and nobody but its author sees it. An author may also send PENDING_REVIEW, which puts it in the moderation queue; PUBLISHED is an editor's to set, and is a 403 otherwise.\n\nThe slug is derived from the title, disambiguated with a numeric suffix if that address is taken, and then fixed for the life of the poem — retitling it later does not move it.\n\n`tags` are slugs of themes that already exist. An unknown one is a 400 naming it: themes are curated, and writing a poem is not how a new one is created.",
        "operationId": "createPoem",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreatePoem"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The poem, as written.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StudioPoem"
                }
              }
            }
          },
          "400": {
            "description": "The body does not match the schema, or names a theme that does not exist."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "Only an editor may publish outright."
          },
          "409": {
            "description": "Two requests raced for the same slug. Nothing was created."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Write a poem",
        "tags": ["poems"]
      }
    },
    "/poems/{slug}": {
      "get": {
        "description": "One published poem by slug, with its full text. A draft, a poem waiting in the queue and a slug nobody has used are all the same 404 — telling a stranger which of the three it was would confirm the poem exists.",
        "operationId": "getPoem",
        "parameters": [
          {
            "name": "slug",
            "required": true,
            "in": "path",
            "description": "The poem's slug.",
            "schema": {
              "example": "tin-nad-polem",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The poem.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Poem"
                }
              }
            }
          },
          "404": {
            "description": "No published poem has that slug."
          }
        },
        "summary": "Get a poem",
        "tags": ["poems"]
      }
    },
    "/poems/{id}": {
      "patch": {
        "description": "Changes only the fields present in the body; at least one is required. An author may change their own poems, an editor anybody's.\n\nThe slug is not writable and does not follow the title: a poem's address is settled when it is created, because a link somebody has shared is a promise. Neither are the author, the read count, or the publication date — the last of which is stamped by the server the first time the poem is published, and kept if it is taken down and put back.\n\n`status: PUBLISHED` and `featured` are both editors' decisions and a 403 otherwise. `status: DRAFT` on a published poem takes it down, which is the reversible act that has to happen before it can be deleted.\n\n`tags` replaces the whole set rather than adding to it, so `[]` files the poem under nothing and omitting the field leaves its themes alone.",
        "operationId": "updatePoem",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The poem's id.",
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdatePoem"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The poem, as changed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StudioPoem"
                }
              }
            }
          },
          "400": {
            "description": "The id or the body does not match the schema."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The poem is somebody else's, or the change is an editor's to make."
          },
          "404": {
            "description": "No poem has that id."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Change a poem",
        "tags": ["poems"]
      },
      "delete": {
        "description": "Permanent, and only available for a poem that is not currently public. A published poem is refused with a 409: readers may have its address, so taking it down (`status: DRAFT`) is a separate and reversible decision that has to be made first.\n\nDeleting takes the poem's themes and its moderation history with it. Deleting a poem that is already gone is a 404, not a no-op.",
        "operationId": "deletePoem",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The poem's id.",
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The poem is gone."
          },
          "400": {
            "description": "The id is not a UUID."
          },
          "401": {
            "description": "No valid access token."
          },
          "403": {
            "description": "The poem is somebody else's."
          },
          "404": {
            "description": "No poem has that id."
          },
          "409": {
            "description": "The poem is published. Take it down first."
          }
        },
        "security": [
          {
            "access-token": []
          }
        ],
        "summary": "Delete a poem",
        "tags": ["poems"]
      }
    }
  },
  "info": {
    "title": "MoodNight API",
    "description": "Read and write endpoints for the MoodNight poetry site. Response shapes come from the zod schemas in @moodnight/shared, the same ones the API validates against.",
    "version": "0.0.0",
    "contact": {}
  },
  "tags": [],
  "servers": [],
  "components": {
    "securitySchemes": {
      "access-token": {
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "type": "http"
      },
      "moodnight_refresh": {
        "type": "apiKey",
        "in": "cookie",
        "name": "moodnight_refresh"
      }
    },
    "schemas": {
      "Health": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": ["ok"]
          },
          "service": {
            "type": "string",
            "description": "Which service answered.",
            "example": "moodnight-api"
          },
          "uptime": {
            "type": "number",
            "minimum": 0,
            "description": "Whole seconds since the process started.",
            "example": 42
          }
        },
        "required": ["status", "service", "uptime"],
        "additionalProperties": false,
        "description": "Liveness payload returned by GET /health."
      },
      "User": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "UUIDv7 — time-ordered, so it sorts by creation."
          },
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Login identity. Unique.",
            "example": "poet@moodnight.dev"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Леся"
          },
          "surname": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Українка"
          },
          "role": {
            "type": "string",
            "enum": ["ROOT", "ADMIN", "EDITOR", "AUTHOR"],
            "description": "What the account is allowed to do. ROOT is the site's owner and there is at most one of them, ADMIN manages users, EDITOR moderates the queue, AUTHOR writes their own poems.",
            "example": "AUTHOR"
          },
          "active": {
            "type": "boolean",
            "description": "Whether the account may sign in. A deactivated account keeps everything it has written — poems already published stay published.",
            "example": true
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When the account was created."
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When the account was last written to."
          }
        },
        "required": ["id", "email", "name", "surname", "role", "active", "createdAt", "updatedAt"],
        "additionalProperties": false,
        "description": "A registered user."
      },
      "CreateUser": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Login identity. Unique.",
            "example": "poet@moodnight.dev"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Леся"
          },
          "surname": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Українка"
          },
          "role": {
            "type": "string",
            "enum": ["ROOT", "ADMIN", "EDITOR", "AUTHOR"],
            "description": "What the account is allowed to do. ROOT is the site's owner and there is at most one of them, ADMIN manages users, EDITOR moderates the queue, AUTHOR writes their own poems.",
            "example": "AUTHOR"
          },
          "password": {
            "type": "string",
            "minLength": 8,
            "maxLength": 128,
            "description": "At least 8 characters, at most 128.",
            "example": "свіча-у-вікні-7"
          }
        },
        "required": ["email", "name", "surname"],
        "additionalProperties": false,
        "description": "The fields needed to create a user."
      },
      "UpdateUser": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Login identity. Unique.",
            "example": "poet@moodnight.dev"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Леся"
          },
          "surname": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Українка"
          },
          "role": {
            "type": "string",
            "enum": ["ROOT", "ADMIN", "EDITOR", "AUTHOR"],
            "description": "What the account is allowed to do. ROOT is the site's owner and there is at most one of them, ADMIN manages users, EDITOR moderates the queue, AUTHOR writes their own poems.",
            "example": "AUTHOR"
          },
          "active": {
            "type": "boolean",
            "description": "Set false to retire an account: it can no longer sign in, and keeps its work.",
            "example": false
          }
        },
        "additionalProperties": false,
        "description": "The fields to change on a user. At least one is required."
      },
      "Register": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "description": "Login identity. Unique.",
            "example": "poet@moodnight.dev"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Леся"
          },
          "surname": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "example": "Українка"
          },
          "password": {
            "type": "string",
            "minLength": 8,
            "maxLength": 128,
            "description": "At least 8 characters, at most 128.",
            "example": "свіча-у-вікні-7"
          }
        },
        "required": ["email", "name", "surname", "password"],
        "additionalProperties": false,
        "description": "The fields needed to register an account."
      },
      "Login": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
            "example": "poet@moodnight.dev"
          },
          "password": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": ["email", "password"],
        "additionalProperties": false,
        "description": "Credentials for signing in."
      },
      "Session": {
        "type": "object",
        "properties": {
          "accessToken": {
            "type": "string",
            "description": "Bearer token for the Authorization header."
          },
          "expiresIn": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "maximum": 9007199254740991,
            "description": "Seconds until the access token expires.",
            "example": 900
          },
          "user": {
            "$ref": "#/components/schemas/User"
          }
        },
        "required": ["accessToken", "expiresIn", "user"],
        "additionalProperties": false,
        "description": "A signed-in session."
      },
      "UserPage": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/User"
            },
            "description": "The rows on this page."
          },
          "total": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many rows match the search and filters, across all pages.",
            "example": 137
          },
          "page": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "maximum": 9007199254740991,
            "description": "Which page this is, counting from 1.",
            "example": 1
          },
          "perPage": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "maximum": 9007199254740991,
            "description": "How many rows a full page holds.",
            "example": 20
          },
          "pageCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many pages `total` divides into. 0 when nothing matches.",
            "example": 7
          }
        },
        "required": ["items", "total", "page", "perPage", "pageCount"],
        "additionalProperties": false,
        "description": "A page of users, and how many match in total."
      },
      "PoemAuthor": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The author's address on the site.",
            "example": "orysia-vechirnia"
          },
          "penName": {
            "type": "string",
            "description": "The name shown to readers.",
            "example": "Орися Вечірня"
          },
          "initials": {
            "type": "string",
            "description": "Up to two letters, for the avatar.",
            "example": "ОВ"
          },
          "roleTitle": {
            "nullable": true,
            "description": "A decorative title, not a permission. Null if the author has none.",
            "example": "Хранитель слова",
            "type": "string"
          },
          "avatarUrl": {
            "nullable": true,
            "description": "Null until Phase 6 gives avatars somewhere to live.",
            "type": "string",
            "format": "uri"
          }
        },
        "required": ["slug", "penName", "initials", "roleTitle", "avatarUrl"],
        "additionalProperties": false,
        "description": "A poem's author, as readers see them."
      },
      "PoemTag": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The displayed Ukrainian name.",
            "example": "Меланхолія"
          },
          "slug": {
            "type": "string",
            "description": "Its address.",
            "example": "melankholiia"
          }
        },
        "required": ["name", "slug"],
        "additionalProperties": false,
        "description": "A theme a poem is filed under."
      },
      "Poem": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "UUIDv7 — time-ordered, so it sorts by creation."
          },
          "slug": {
            "type": "string",
            "description": "The poem's address.",
            "example": "tin-nad-polem"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "example": "Тінь над полем"
          },
          "subtitle": {
            "nullable": true,
            "description": "The line under the title, if any.",
            "example": "із циклу «Спалені листи»",
            "type": "string",
            "maxLength": 200
          },
          "author": {
            "$ref": "#/components/schemas/PoemAuthor"
          },
          "tags": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PoemTag"
            },
            "description": "Themes, possibly none."
          },
          "publishedAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When the poem became public."
          },
          "readCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many times it has been opened.",
            "example": 1247
          },
          "featured": {
            "type": "boolean",
            "description": "Whether an editor has put it on the front page.",
            "example": false
          },
          "body": {
            "type": "string",
            "description": "The poem, newline-separated, as its author typed it. Plain text — the line breaks are content, so a client renders them and does not reflow them."
          }
        },
        "required": [
          "id",
          "slug",
          "title",
          "subtitle",
          "author",
          "tags",
          "publishedAt",
          "readCount",
          "featured",
          "body"
        ],
        "additionalProperties": false,
        "description": "A poem in full."
      },
      "PoemSummary": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "UUIDv7 — time-ordered, so it sorts by creation."
          },
          "slug": {
            "type": "string",
            "description": "The poem's address.",
            "example": "tin-nad-polem"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "example": "Тінь над полем"
          },
          "subtitle": {
            "nullable": true,
            "description": "The line under the title, if any.",
            "example": "із циклу «Спалені листи»",
            "type": "string",
            "maxLength": 200
          },
          "author": {
            "$ref": "#/components/schemas/PoemAuthor"
          },
          "tags": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PoemTag"
            },
            "description": "Themes, possibly none."
          },
          "publishedAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When the poem became public."
          },
          "readCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many times it has been opened.",
            "example": 1247
          },
          "featured": {
            "type": "boolean",
            "description": "Whether an editor has put it on the front page.",
            "example": false
          },
          "teaser": {
            "type": "string",
            "description": "The first 6 lines, newline-separated."
          },
          "truncated": {
            "type": "boolean",
            "description": "Whether the poem is longer than the teaser shows.",
            "example": true
          }
        },
        "required": [
          "id",
          "slug",
          "title",
          "subtitle",
          "author",
          "tags",
          "publishedAt",
          "readCount",
          "featured",
          "teaser",
          "truncated"
        ],
        "additionalProperties": false,
        "description": "A poem as a card in the feed shows it."
      },
      "PoemPage": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PoemSummary"
            },
            "description": "The rows on this page."
          },
          "total": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many rows match the search and filters, across all pages.",
            "example": 137
          },
          "page": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "maximum": 9007199254740991,
            "description": "Which page this is, counting from 1.",
            "example": 1
          },
          "perPage": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true,
            "maximum": 9007199254740991,
            "description": "How many rows a full page holds.",
            "example": 20
          },
          "pageCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many pages `total` divides into. 0 when nothing matches.",
            "example": 7
          }
        },
        "required": ["items", "total", "page", "perPage", "pageCount"],
        "additionalProperties": false,
        "description": "A page of poems, and how many match in total."
      },
      "StudioPoem": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
            "description": "UUIDv7 — time-ordered, so it sorts by creation."
          },
          "slug": {
            "type": "string",
            "description": "The poem's address.",
            "example": "tin-nad-polem"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "example": "Тінь над полем"
          },
          "subtitle": {
            "nullable": true,
            "description": "The line under the title, if any.",
            "example": "із циклу «Спалені листи»",
            "type": "string",
            "maxLength": 200
          },
          "author": {
            "$ref": "#/components/schemas/PoemAuthor"
          },
          "tags": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PoemTag"
            },
            "description": "Themes, possibly none."
          },
          "readCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "How many times it has been opened.",
            "example": 1247
          },
          "featured": {
            "type": "boolean",
            "description": "Whether an editor has put it on the front page.",
            "example": false
          },
          "body": {
            "type": "string",
            "description": "The poem, newline-separated."
          },
          "status": {
            "type": "string",
            "enum": ["DRAFT", "PENDING_REVIEW", "PUBLISHED", "REJECTED"],
            "description": "Where the poem is on its way to being public. Only PUBLISHED poems are returned by the public endpoints.",
            "example": "PUBLISHED"
          },
          "publishedAt": {
            "nullable": true,
            "description": "When the poem became public, or null if it never has.",
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When the poem was first written."
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time",
            "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
            "description": "When it was last saved."
          }
        },
        "required": [
          "id",
          "slug",
          "title",
          "subtitle",
          "author",
          "tags",
          "readCount",
          "featured",
          "body",
          "status",
          "publishedAt",
          "createdAt",
          "updatedAt"
        ],
        "additionalProperties": false,
        "description": "A poem as its author sees it, drafts included."
      },
      "CreatePoem": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "example": "Тінь над полем"
          },
          "subtitle": {
            "nullable": true,
            "description": "The line under the title, if any.",
            "example": "із циклу «Спалені листи»",
            "type": "string",
            "maxLength": 200
          },
          "body": {
            "type": "string",
            "minLength": 1,
            "maxLength": 20000,
            "description": "The poem, newline-separated, as it should be read. Plain text — the line breaks are content, not formatting.",
            "example": "Тінь над полем лягла,\nі вітер затих."
          },
          "tags": {
            "description": "Slugs of existing tags, at most 6. Replaces the whole set.",
            "example": ["melankholiia", "nich"],
            "maxItems": 6,
            "type": "array",
            "items": {
              "type": "string",
              "maxLength": 80
            }
          },
          "status": {
            "type": "string",
            "enum": ["DRAFT", "PENDING_REVIEW", "PUBLISHED"],
            "description": "DRAFT keeps the poem private, PENDING_REVIEW submits it to the queue, and PUBLISHED makes it public — the last of which is an editor's to set. Rejecting is not here: it carries a note, and that is the queue's own endpoint.",
            "example": "PENDING_REVIEW"
          }
        },
        "required": ["title", "body"],
        "additionalProperties": false,
        "description": "The fields needed to create a poem."
      },
      "UpdatePoem": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "example": "Тінь над полем"
          },
          "subtitle": {
            "nullable": true,
            "description": "The line under the title, if any.",
            "example": "із циклу «Спалені листи»",
            "type": "string",
            "maxLength": 200
          },
          "body": {
            "type": "string",
            "minLength": 1,
            "maxLength": 20000,
            "description": "The poem, newline-separated, as it should be read. Plain text — the line breaks are content, not formatting.",
            "example": "Тінь над полем лягла,\nі вітер затих."
          },
          "tags": {
            "description": "Slugs of existing tags, at most 6. Replaces the whole set.",
            "example": ["melankholiia", "nich"],
            "maxItems": 6,
            "type": "array",
            "items": {
              "type": "string",
              "maxLength": 80
            }
          },
          "status": {
            "type": "string",
            "enum": ["DRAFT", "PENDING_REVIEW", "PUBLISHED"],
            "description": "DRAFT keeps the poem private, PENDING_REVIEW submits it to the queue, and PUBLISHED makes it public — the last of which is an editor's to set. Rejecting is not here: it carries a note, and that is the queue's own endpoint.",
            "example": "PENDING_REVIEW"
          },
          "featured": {
            "type": "boolean",
            "description": "Whether the poem sits on the front page. Editors and above.",
            "example": true
          }
        },
        "additionalProperties": false,
        "description": "The fields to change on a poem. At least one is required."
      }
    }
  }
}
