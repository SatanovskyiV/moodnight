/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * MoodNight API
 * Read and write endpoints for the MoodNight poetry site. Response shapes come from the zod schemas in @moodnight/shared, the same ones the API validates against.
 * OpenAPI spec version: 0.0.0
 */
import type { CreatePoemStatus } from "./createPoemStatus";

/**
 * The fields needed to create a poem.
 */
export interface CreatePoem {
  /**
   * @minLength 1
   * @maxLength 200
   */
  title: string;
  /**
   * The line under the title, if any.
   * @maxLength 200
   * @nullable
   */
  subtitle?: string | null;
  /**
   * The poem, newline-separated, as it should be read. Plain text — the line breaks are content, not formatting.
   * @minLength 1
   * @maxLength 20000
   */
  body: string;
  /**
   * Slugs of existing tags, at most 6. Replaces the whole set.
   * @maxItems 6
   * @items.maxLength 80
   */
  tags?: string[];
  /** DRAFT keeps the poem private, PENDING_REVIEW submits it to the queue, and PUBLISHED makes it public — the last of which is an editor's to set. Rejecting is not here: it carries a note, and that is the queue's own endpoint. */
  status?: CreatePoemStatus;
}
